interface Account {
	NO: string;
	UID: string;
	PASSWORD: string;
}

interface Content {
	NO: string;
	COOKIE: string;
	IDFANSPAGE: string;
	PATH: string;
	CAPTION: string;
}

export { type Account, type Content };
